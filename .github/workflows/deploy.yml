name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - claude/fix-system-errors-EDe6q

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  # الخطوة 1: عند Push على فرع claude — انسخ التغييرات لـ main
  sync-to-main:
    if: github.ref == 'refs/heads/claude/fix-system-errors-EDe6q'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Copy index.html to main and push
        run: |
          git config user.email "github-actions@github.com"
          git config user.name "github-actions[bot]"
          git checkout main
          git checkout claude/fix-system-errors-EDe6q -- index.html
          git add index.html
          git diff --cached --quiet || git commit -m "chore: مزامنة التحديثات من فرع التطوير تلقائياً"
          git push origin main

  # الخطوة 2: عند Push على main — انشر الموقع
  deploy:
    if: github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
